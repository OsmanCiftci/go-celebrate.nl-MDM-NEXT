'use strict';

/**
 * attribute-definition service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::attribute-definition.attribute-definition');
